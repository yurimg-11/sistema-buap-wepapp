import { Routes } from '@angular/router';
import { LoginScreenComponent } from './screens/login-screen/login-screen.component'; // Corrected path

export const routes: Routes = [
  { path: 'login', component: LoginScreenComponent },
];
